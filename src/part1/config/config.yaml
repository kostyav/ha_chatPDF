engine: ollama          # one of: ollama | llamacpp | vllm
model: gemma3:1b        # engine-specific model name / HF id / path
quantization: ""        # optional: dash-appended to ollama tag (e.g. q4_K_M), --quantization for vllm, ignored for llamacpp gguf paths
