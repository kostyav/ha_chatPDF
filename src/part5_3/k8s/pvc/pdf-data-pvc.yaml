apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pdf-data
  namespace: rag-system
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi
