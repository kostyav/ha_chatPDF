apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: byaldi-index
  namespace: rag-system
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
