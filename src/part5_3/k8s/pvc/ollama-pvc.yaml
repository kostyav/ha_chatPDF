apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
  namespace: rag-system
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
