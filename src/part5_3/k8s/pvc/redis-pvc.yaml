apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: rag-system
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard   # minikube default (hostPath)
  resources:
    requests:
      storage: 1Gi
