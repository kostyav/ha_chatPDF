apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: rag-system
spec:
  selector:
    app: api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30808   # access via: minikube service api -n rag-system --url
  type: NodePort
